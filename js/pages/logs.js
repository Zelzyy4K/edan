import {state,updateState} from '../state.js';import {confirmAction} from '../components/confirmation.js';import {toast} from '../components/toast.js';
export function renderLogs(){const root=document.createElement('div');let filter='All';root.innerHTML=`<div class="page-head"><div><span class="eyebrow">System</span><h1>Activity Logs</h1><p>Every simulated action is recorded here.</p></div><button class="btn btn-danger" id="clear"><i data-lucide="trash-2"></i> Clear Logs</button></div><div class="toolbar"><div class="filter-row" id="log-filters">${['All','Success','Failed','Warning','Injection','Bypass','Backup','Fix','Steam'].map(x=>`<button class="filter-chip ${x==='All'?'active':''}" data-filter="${x}">${x}</button>`).join('')}</div></div><section class="panel"><div id="log-list" class="log-list"></div></section>`;const draw=()=>{const logs=state.logs.filter(x=>filter==='All'||x.status===filter.toLowerCase()||x.type===filter);root.querySelector('#log-list').innerHTML=logs.length?logs.map(x=>`<div class="log-row"><div class="log-icon ${x.status}"><i data-lucide="${x.status==='success'?'check':x.status==='warning'?'triangle-alert':'x'}"></i></div><div class="log-copy"><div><strong>${x.game}</strong><span>${x.action}</span></div><p>${x.detail}</p></div><div class="log-time">${x.time}<small>${x.type}</small></div></div>`).join(''):`<div class="empty-state"><i data-lucide="scroll-text"></i><h3>No matching activity</h3><p>Run a tool or change the filter.</p></div>`;window.lucide?.createIcons({nodes:[root]});};root.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{filter=b.dataset.filter;root.querySelectorAll('[data-filter]').forEach(x=>x.classList.toggle('active',x===b));draw()});root.querySelector('#clear').onclick=()=>confirmAction({title:'Clear Activity Logs?',message:'This will remove all demo activity history.',confirmLabel:'Clear Logs',onConfirm:()=>{updateState(s=>s.logs=[]);toast('Activity logs cleared','success');draw()}});draw();return {element:root};}
