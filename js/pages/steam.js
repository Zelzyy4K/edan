import {state} from '../state.js';import {runProgress} from '../components/progress.js';import {toast} from '../components/toast.js';import {steamService} from '../services/mockSteam.js';
export function renderSteam(){const root=document.createElement('div');root.innerHTML=`<div class="page-head"><div><span class="eyebrow">Steam</span><h1>Steam Management</h1><p>Monitor and simulate Steam maintenance actions.</p></div></div><div class="steam-grid"><section class="panel status-panel"><div class="steam-status"><span class="status-light ${state.steam.running?'online':'offline'}"></span><div><h2>Steam Status</h2><p>${state.steam.running?'Running':'Stopped'}</p></div></div><button class="btn btn-primary" id="refresh"><i data-lucide="refresh-cw"></i> Refresh Status</button></section><section class="panel"><div class="panel-head"><div><h2>Steam Installation</h2><p>Demo path shown for UI testing.</p></div></div><label class="field"><span>Steam Path</span><input value="${state.steam.path}" readonly></label></section></div><section class="panel"><div class="panel-head"><div><h2>Steam Actions</h2><p>All actions below are simulated in the browser.</p></div></div><div class="steam-actions"><button class="btn btn-secondary" id="restart"><i data-lucide="power"></i>Restart Steam</button><button class="btn btn-secondary" onclick="void(0)" id="folder"><i data-lucide="folder-open"></i>Open Steam Folder</button><button class="btn btn-secondary" id="open"><i data-lucide="external-link"></i>Open Steam</button></div></section>`;root.querySelector('#restart').onclick=()=>runProgress({title:'Restart Steam',steps:['Stopping Steam...','Waiting...','Starting Steam...','Checking status...'],onComplete:async()=>{await steamService.restart();toast('Steam restart simulation completed','success')}});root.querySelector('#refresh').onclick=async()=>{const result=await steamService.status();toast(result.running?'Steam is running':'Steam is not running','success')};root.querySelector('#folder').onclick=()=>toast('Folder opening is unavailable in browser mock mode','info');root.querySelector('#open').onclick=()=>toast('Launching Steam is unavailable in browser mock mode','info');window.lucide?.createIcons({nodes:[root]});return {element:root};}
