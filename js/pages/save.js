import {state} from '../state.js';import {confirmAction} from '../components/confirmation.js';import {runProgress} from '../components/progress.js';import {toast} from '../components/toast.js';import {createBackup,restoreBackup,deleteBackup} from '../services/mockBackup.js';
export function renderSave(){const root=document.createElement('div');root.innerHTML=`<div class="page-head"><div><span class="eyebrow">Data</span><h1>Save & Restore</h1><p>Protect game data with local demo backups and simulated cloud connection.</p></div></div><div class="backup-grid"><section class="panel"><div class="panel-head"><div><h2>Local Backup</h2><p>Select a game and create a simulated archive.</p></div></div><div class="backup-form"><label class="field"><span>Game</span><select id="backup-game">${state.games.filter(g=>g.installed).map(g=>`<option value="${g.id}">${g.name}</option>`).join('')}</select></label><label class="field"><span>Backup location</span><input value="Local Demo Storage" disabled></label><button class="btn btn-primary" id="create-backup"><i data-lucide="archive"></i>Create Backup</button></div></section><section class="panel cloud-panel"><div class="cloud-icon"><i data-lucide="cloud"></i></div><div><h2>Cloud Backup</h2><p>Google Drive / Cloud connection is simulated.</p><span class="badge badge-muted">Not Connected</span></div><button class="btn btn-secondary" id="connect-cloud">Connect Cloud</button></section></div><section class="panel"><div class="panel-head"><div><h2>Backup List</h2><p>${state.backups.length} demo backups available.</p></div></div><div class="table-wrap"><table><thead><tr><th>Game</th><th>Backup</th><th>Date</th><th>Size</th><th>Location</th><th>Status</th><th></th></tr></thead><tbody>${state.backups.length?state.backups.map(b=>`<tr><td>${b.game}</td><td>${b.name}</td><td>${b.date}</td><td>${b.size}</td><td>${b.location}</td><td><span class="badge badge-success">${b.status}</span></td><td><div class="row-actions"><button class="icon-btn restore" data-id="${b.id}" aria-label="Restore"><i data-lucide="history"></i></button><button class="icon-btn delete" data-id="${b.id}" aria-label="Delete"><i data-lucide="trash-2"></i></button></div></td></tr>`).join(''):`<tr><td colspan="7"><div class="empty-state compact"><i data-lucide="archive"></i><h3>No backups yet</h3><p>Create your first backup to protect your game data.</p></div></td></tr>`}</tbody></table></div></section>`;root.querySelector('#create-backup').onclick=()=>{const game=state.games.find(g=>g.id===root.querySelector('#backup-game').value);runProgress({title:'Creating Backup',steps:['Preparing backup...','Collecting save data...','Compressing demo archive...','Verifying backup...'],onComplete:async()=>{await createBackup(game);toast('Backup created successfully','success')}})};root.querySelector('#connect-cloud').onclick=()=>toast('Demo cloud connection successful','success');root.querySelectorAll('.restore').forEach(b=>b.onclick=()=>{const backup=state.backups.find(x=>x.id==b.dataset.id);confirmAction({title:'Restore Backup?',message:`Restore ${backup.name}? Existing demo data may be replaced.`,confirmLabel:'Restore',onConfirm:()=>runProgress({title:'Restoring Backup',steps:['Checking backup...','Preparing restore...','Restoring demo files...','Verification...'],onComplete:async()=>{await restoreBackup(backup);toast('Backup restored successfully','success')}})});});root.querySelectorAll('.delete').forEach(b=>b.onclick=()=>{const backup=state.backups.find(x=>x.id==b.dataset.id);confirmAction({title:'Delete Backup?',message:`Delete ${backup.name} from demo history?`,confirmLabel:'Delete',onConfirm:async()=>{await deleteBackup(backup);toast('Backup deleted','success')}})});window.lucide?.createIcons({nodes:[root]});return {element:root};}
